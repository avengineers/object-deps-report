# CHANGELOG

<!-- version list -->

## v0.1.0

- Initial release: `objects-deps` CMake target that runs `clanguru analyze` to
  generate the object-file dependency graph, depending on the linked executable
  and `compile_commands.json`.
